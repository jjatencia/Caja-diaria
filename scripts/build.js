#!/usr/bin/env node

// Simple build script for Vercel compatibility
// Since this is a vanilla JS app, no build is actually needed

console.log('🚀 Vercel build process...');
console.log('✅ No build needed for vanilla JavaScript app');
console.log('📦 Static files will be served directly');
console.log('🔧 API functions will be handled by Vercel serverless');

// Exit successfully
process.exit(0);